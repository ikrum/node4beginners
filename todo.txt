Target:
  socket
  Redis+jobQueue
  Testing and deployment, docker
  AngularJS

JOB QUEUE:
  1) Kue
  2) Gearman

  const kue = require('kue');
  const ui = require('kue-ui');

  /* Job queue */
  kue.createQueue({redis: config.REDIS_RUL});
  ui.setup({
      apiURL: '/kue_api', // IMPORTANT: specify the api url, Expose api for Kue UI
      baseURL: '/kue', // IMPORTANT: specify the base url
      updateInterval: 5000 // Optional: Fetches new data every 5000 ms
  });
  kue.app.set('title', 'SM v2 Queue');
  app.use('/kue_api', kue.app); // Mount kue JSON api
  app.use('/kue', ui.app); // Mount UI
